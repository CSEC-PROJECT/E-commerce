openapi: 3.0.0
info:
  title: E-Commerce API
  version: 1.0.0
  description: Authentication and Admin Product APIs

servers:
  - url: http://localhost:3000
  - url: https://e-commerce-he4h.onrender.com


tags:
  - name: Auth
    description: Authentication endpoints
  - name: Admin Products
    description: Admin product management
  - name: Admin Users
    description: Admin user management
  - name: Products
    description: Public product endpoints
  - name: Admin Orders
    description: Admin order management
  - name: Orders
    description: User order management
  - name: Cart
    description: User shopping cart management
  - name: Admin Dashboard
    description: Admin dashboard endpoints
  - name: Admin Finance
    description: Admin finance endpoints
  - name: Payment
    description: Payment processing endpoints

components:
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT

  schemas:
    User:
      type: object
      properties:
        _id:
          type: string
        name:
          type: string
        email:
          type: string
        role:
          type: array
          items:
            type: string
        isVerified:
          type: boolean
        profilePic:
          type: string
        cloudinaryId:
          type: string
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time

    Order:
      type: object
      properties:
        _id:
          type: string
        userId:
          type: string
        items:
          type: array
          items:
            type: object
            properties:
              productId:
                type: string
              quantity:
                type: number
        totalPrice:
          type: number
        status:
          type: string
          enum: [pending, paid, shipped, delivered]
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time

    CartItem:
      type: object
      required:
        - product
        - quantity
        - price
      properties:
        product:
          oneOf:
            - type: string
              description: The Product ID
            - $ref: '#/components/schemas/Product'
        quantity:
          type: number
          minimum: 1
          default: 1
        price:
          type: number
          minimum: 0

    Cart:
      type: object
      properties:
        _id:
          type: string
        user:
          type: string
        items:
          type: array
          items:
            $ref: '#/components/schemas/CartItem'
        totalPrice:
          type: number
          default: 0
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time

    Product:
      type: object
      properties:
        _id:
          type: string
        name:
          type: string
        description:
          type: string
        price:
          type: number
        category:
          type: string
        stock:
          type: number
        status:
          type: string
        madeIn:
          type: string
        material:
          type: string
        coverImage:
          type: string
        detailImages:
          type: array
          items:
            type: string

paths:

  /api/auth/register:
    post:
      tags: [Auth]
      summary: Register User
      description: Register a new user with profile picture and send email verification.
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - name
                - email
                - password
              properties:
                name:
                  type: string
                email:
                  type: string
                password:
                  type: string
                profilePic:
                  type: string
                  format: binary
      responses:
        "201":
          description: User registered successfully
          content:
            application/json:
              schema:
                type: object
                properties:
                  message:
                    type: string
                  data:
                    type: object
                    properties:
                      _id:
                        type: string
                      name:
                        type: string
                      email:
                        type: string
                      role:
                        type: array
                        items:
                          type: string
                      isVerified:
                        type: boolean
                      profilePic:
                        type: string
                      cloudinaryId:
                        type: string
                      createdAt:
                        type: string
                        format: date-time
                      updatedAt:
                        type: string
                        format: date-time
                  accessToken:
                    type: string
        "400":
          description: Bad request

  /api/auth/verify:
    get:
      tags: [Auth]
      summary: Verify Email
      description: Verify user email via token sent to email.
      parameters:
        - in: query
          name: token
          required: true
          schema:
            type: string
      responses:
        "302":
          description: Redirect to frontend login page
        "400":
          description: Invalid or missing token

  /api/auth/login:
    post:
      tags: [Auth]
      summary: Login User
      description: Authenticate user and return tokens.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - email
                - password
              properties:
                email:
                  type: string
                password:
                  type: string
      responses:
        "200":
          description: Login successful
          content:
            application/json:
              schema:
                type: object
                properties:
                  message:
                    type: string
                  data:
                    type: object
                    properties:
                      _id:
                        type: string
                      name:
                        type: string
                      email:
                        type: string
                      role:
                        type: array
                        items:
                          type: string
                      isVerified:
                        type: boolean
                      profilePic:
                        type: string
                  accessToken:
                    type: string
                  refreshToken:
                    type: string
        "400":
          description: Invalid credentials
        "401":
          description: Email not verified

  /api/auth/refresh:
    post:
      tags: [Auth]
      summary: Refresh Token
      description: Generate new access and refresh tokens.
      requestBody:
        required: false
        content:
          application/json:
            schema:
              type: object
              properties:
                refreshToken:
                  type: string
      responses:
        "200":
          description: Tokens refreshed successfully
          content:
            application/json:
              schema:
                type: object
                properties:
                  accessToken:
                    type: string
                  refreshToken:
                    type: string
        "400":
          description: Refresh token required
        "401":
          description: Invalid refresh token

  /api/auth/logout:
    post:
      tags: [Auth]
      summary: Logout User
      description: Logout user and invalidate refresh token.
      requestBody:
        required: false
        content:
          application/json:
            schema:
              type: object
              properties:
                refreshToken:
                  type: string
      responses:
        "200":
          description: Logged out successfully

  /api/auth/password-reset/request:
    post:
      tags: [Auth]
      summary: Request Password Reset
      description: Send password reset link to email.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - email
              properties:
                email:
                  type: string
      responses:
        "200":
          description: Reset link sent if email exists

  /api/auth/password-reset/confirm:
    post:
      tags: [Auth]
      summary: Confirm Password Reset
      description: Reset password using token.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - token
                - newPassword
              properties:
                token:
                  type: string
                newPassword:
                  type: string
      responses:
        "200":
          description: Password reset successful
        "400":
          description: Invalid or expired token


  /api/admin/products:
    post:
      tags: [Admin Products]
      summary: Create Product (Admin)
      description: Create a new product with images (Admin only).
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - name
                - description
                - category
                - price
                - stock
                - coverImage
              properties:
                name:
                  type: string
                description:
                  type: string
                category:
                  type: string
                price:
                  type: number
                stock:
                  type: number
                status:
                  type: string
                  enum: [used, slightly used, new]
                madeIn:
                  type: string
                material:
                  type: string
                coverImage:
                  type: string
                  format: binary
                detailImages:
                  type: array
                  items:
                    type: string
                    format: binary
      responses:
        "201":
          description: Product created successfully
          content:
            application/json:
              schema:
                type: object
                properties:
                  message:
                    type: string
                  product:
                    type: object
        "400":
          description: Validation error
        "401":
          description: Unauthorized
        "403":
          description: Forbidden (Admin only)

  /api/admin/dashboard/stats:
    get:
      tags: [Admin Dashboard]
      summary: Get Dashboard Stats
      description: Retrieve key statistics for the admin dashboard.
      security:
        - bearerAuth: []
      responses:
        "200":
          description: Successfully retrieved dashboard stats.
          content:
            application/json:
              schema:
                type: object
                properties:
                  totalUsers:
                    type: integer
                  totalProducts:
                    type: integer
                  totalOrders:
                    type: integer
                  totalRevenue:
                    type: number
        "401":
          description: Unauthorized
        "403":
          description: Forbidden (Admin only)

  /api/admin/dashboard/sales-performance:
    get:
      tags: [Admin Dashboard]
      summary: Get Weekly Sales Performance
      description: Retrieve sales performance data for the last week.
      security:
        - bearerAuth: []
      responses:
        "200":
          description: Successfully retrieved weekly sales performance.
          content:
            application/json:
              schema:
                type: array
                items:
                  type: object
                  properties:
                    day:
                      type: string
                    sales:
                      type: number
        "401":
          description: Unauthorized
        "403":
          description: Forbidden (Admin only)

  /api/admin/dashboard/recent-transactions:
    get:
      tags: [Admin Dashboard]
      summary: Get Recent Transactions
      description: Retrieve a list of recent transactions.
      security:
        - bearerAuth: []
      responses:
        "200":
          description: Successfully retrieved recent transactions.
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/Order'
        "401":
          description: Unauthorized
        "403":
          description: Forbidden (Admin only)

  /api/admin/dashboard/best-selling:
    get:
      tags: [Admin Dashboard]
      summary: Get Best-Selling Products
      description: Retrieve a list of the best-selling products.
      security:
        - bearerAuth: []
      responses:
        "200":
          description: Successfully retrieved best-selling products.
          content:
            application/json:
              schema:
                type: array
                items:
                  type: object
                  properties:
                    productId:
                      type: string
                    name:
                      type: string
                    totalSold:
                      type: integer
        "401":
          description: Unauthorized
        "403":
          description: Forbidden (Admin only)

  /api/admin/finance/overview:
    get:
      tags: [Admin Finance]
      summary: Get Financial Overview
      description: Retrieve a financial overview including total revenue, refunds, and net income.
      security:
        - bearerAuth: []
      responses:
        "200":
          description: Successfully retrieved financial overview.
          content:
            application/json:
              schema:
                type: object
                properties:
                  totalRevenue:
                    type: number
                  totalRefunds:
                    type: number
                  netIncome:
                    type: number
        "401":
          description: Unauthorized
        "403":
          description: Forbidden (Admin only)

  /api/admin/finance/revenue-over-time:
    get:
      tags: [Admin Finance]
      summary: Get Revenue Over Time
      description: Retrieve revenue data over a specified period.
      security:
        - bearerAuth: []
      responses:
        "200":
          description: Successfully retrieved revenue data.
          content:
            application/json:
              schema:
                type: array
                items:
                  type: object
                  properties:
                    date:
                      type: string
                      format: date
                    revenue:
                      type: number
        "401":
          description: Unauthorized
        "403":
          description: Forbidden (Admin only)

  /api/admin/finance/orders-vs-refunds:
    get:
      tags: [Admin Finance]
      summary: Get Orders vs. Refunds
      description: Compare the number of orders and refunds over time.
      security:
        - bearerAuth: []
      responses:
        "200":
          description: Successfully retrieved orders vs. refunds data.
          content:
            application/json:
              schema:
                type: array
                items:
                  type: object
                  properties:
                    month:
                      type: string
                    orders:
                      type: integer
                    refunds:
                      type: integer
        "401":
          description: Unauthorized
        "403":
          description: Forbidden (Admin only)

  /api/admin/finance/transactions:
    get:
      tags: [Admin Finance]
      summary: Get All Financial Transactions
      description: Retrieve a list of all financial transactions.
      security:
        - bearerAuth: []
      responses:
        "200":
          description: Successfully retrieved all transactions.
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/Order'
        "401":
          description: Unauthorized
        "403":
          description: Forbidden (Admin only)

  /api/admin/products/{id}:
    put:
      tags: [Admin Products]
      summary: Update Product (Admin)
      description: Update product details and images.
      security:
        - bearerAuth: []
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
          description: Product ID
      requestBody:
        required: false
        content:
          multipart/form-data:
            schema:
              type: object
              properties:
                name:
                  type: string
                description:
                  type: string
                category:
                  type: string
                price:
                  type: number
                stock:
                  type: number
                status:
                  type: string
                  enum: [used, slightly used, new]
                madeIn:
                  type: string
                material:
                  type: string
                coverImage:
                  type: string
                  format: binary
                detailImages:
                  type: array
                  items:
                    type: string
                    format: binary
      responses:
        "200":
          description: Product updated successfully
          content:
            application/json:
              schema:
                type: object
                properties:
                  message:
                    type: string
                  product:
                    type: object
        "400":
          description: Invalid product ID or validation error
        "404":
          description: Product not found
        "401":
          description: Unauthorized
        "403":
          description: Forbidden (Admin only)

    delete:
      tags: [Admin Products]
      summary: Delete Product (Admin)
      description: Delete a product by ID.
      security:
        - bearerAuth: []
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
          description: Product ID
      responses:
        "200":
          description: Product deleted successfully
        "400":
          description: Invalid product ID
        "404":
          description: Product not found
        "401":
          description: Unauthorized
        "403":
          description: Forbidden (Admin only)

  /api/products:
    get:
      tags: [Products]
      summary: Get Products
      description: Fetch all products with optional filtering, search, and pagination.
      parameters:
        - in: query
          name: search
          schema:
            type: string
          description: Search by product name
        - in: query
          name: category
          schema:
            type: string
          description: Filter by category
        - in: query
          name: status
          schema:
            type: string
            enum: [used, slightly used, new]
          description: Filter by product status
        - in: query
          name: minPrice
          schema:
            type: number
          description: Minimum price
        - in: query
          name: maxPrice
          schema:
            type: number
          description: Maximum price
        - in: query
          name: page
          schema:
            type: integer
            default: 1
          description: Page number
        - in: query
          name: limit
          schema:
            type: integer
            default: 10
          description: Number of products per page
      responses:
        "200":
          description: Products fetched successfully
          content:
            application/json:
              schema:
                type: object
                properties:
                  products:
                    type: array
                    items:
                      $ref: '#/components/schemas/Product'
        "500":
          description: Server error

  /api/products/{id}:
    get:
      tags: [Products]
      summary: Get Product by ID
      description: Fetch a single product by its ID.
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
          description: Product ID
      responses:
        "200":
          description: Product fetched successfully
          content:
            application/json:
              schema:
                type: object
                properties:
                  product:
                    $ref: '#/components/schemas/Product'
        "400":
          description: Invalid product ID
        "404":
          description: Product not found
        "500":
          description: Server error


  /api/admin/orders:
    get:
      tags: [Admin Orders]
      summary: Get All Orders (Admin)
      description: Fetch all orders with user details (Admin only).
      security:
        - bearerAuth: []
      responses:
        "200":
          description: Orders fetched successfully
          content:
            application/json:
              schema:
                type: object
                properties:
                  orders:
                    type: array
                    items:
                      $ref: '#/components/schemas/Order'
        "401":
          description: Unauthorized
        "403":
          description: Forbidden (Admin only)
        "500":
          description: Server error

  /api/admin/orders/{id}:
    put:
      tags: [Admin Orders]
      summary: Update Order Status (Admin)
      description: Update the status of an order.
      security:
        - bearerAuth: []
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
          description: Order ID
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - status
              properties:
                status:
                  type: string
                  enum: [pending, paid, shipped, delivered]
      responses:
        "200":
          description: Order status updated successfully
          content:
            application/json:
              schema:
                type: object
                properties:
                  message:
                    type: string
                  order:
                    $ref: '#/components/schemas/Order'
        "400":
          description: Invalid order ID or status
        "404":
          description: Order not found
        "401":
          description: Unauthorized
        "403":
          description: Forbidden (Admin only)
        "500":
          description: Server error


  /api/user/orders:
    post:
      tags: [Orders]
      summary: Create Order
      description: Create a new order for the authenticated user.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - items
                - totalPrice
              properties:
                items:
                  type: array
                  items:
                    type: object
                    properties:
                      productId:
                        type: string
                      quantity:
                        type: number
                totalPrice:
                  type: number
                status:
                  type: string
                  enum: [pending, paid, shipped, delivered]
      responses:
        "201":
          description: Order created successfully
          content:
            application/json:
              schema:
                type: object
                properties:
                  message:
                    type: string
                  order:
                    $ref: '#/components/schemas/Order'
        "400":
          description: Missing required fields
        "401":
          description: Unauthorized
        "500":
          description: Server error

  /api/user/orders/my:
    get:
      tags: [Orders]
      summary: Get My Orders
      description: Fetch all orders of the authenticated user.
      security:
        - bearerAuth: []
      responses:
        "200":
          description: Orders fetched successfully
          content:
            application/json:
              schema:
                type: object
                properties:
                  orders:
                    type: array
                    items:
                      $ref: '#/components/schemas/Order'
        "401":
          description: Unauthorized
        "500":
          description: Server error

  /api/payment:
    post:
      tags: [Payment]
      summary: Initialize Payment
      description: Initialize a payment transaction for an order.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - orderId
              properties:
                orderId:
                  type: string
                  description: The ID of the order to pay for.
      responses:
        "200":
          description: Payment initialized successfully.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                  message:
                    type: string
                  data:
                    type: object
                    properties:
                      authorization_url:
                        type: string
                      access_code:
                        type: string
                      reference:
                        type: string
        "401":
          description: Unauthorized
        "404":
          description: Order not found
        "500":
          description: Server error

  /api/payment/verify/{tx_ref}:
    get:
      tags: [Payment]
      summary: Verify Payment
      description: Verify the status of a payment transaction.
      parameters:
        - in: path
          name: tx_ref
          required: true
          schema:
            type: string
          description: The transaction reference.
      responses:
        "302":
          description: Redirects to a success or failure page.
        "400":
          description: Invalid transaction reference.
        "500":
          description: Server error

  /api/payment/webhook:
    post:
      tags: [Payment]
      summary: Payment Webhook
      description: Handles webhook notifications from the payment gateway.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              description: The webhook payload from the payment provider.
      responses:
        "200":
          description: Webhook received successfully.

  /api/paycheck/success/{tx_ref}:
    get:
      tags: [Payment]
      summary: Payment Success
      description: Display a success message to the user after a successful payment.
      parameters:
        - in: path
          name: tx_ref
          required: true
          schema:
            type: string
          description: The transaction reference.
      responses:
        "200":
          description: Success message.
          content:
            application/json:
              schema:
                type: object
                properties:
                  message:
                    type: string
                  transaction:
                    type: object
        "404":
          description: Transaction not found.
        "500":
          description: Server error

  /api/user/orders/{id}:
    get:
      tags: [Orders]
      summary: Get Order by ID
      description: Fetch a single order (only owner or admin can access).
      security:
        - bearerAuth: []
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
          description: Order ID
      responses:
        "200":
          description: Order fetched successfully
          content:
            application/json:
              schema:
                type: object
                properties:
                  order:
                    $ref: '#/components/schemas/Order'
        "400":
          description: Invalid order ID
        "401":
          description: Unauthorized
        "403":
          description: Forbidden
        "404":
          description: Order not found
        "500":
          description: Server error


  /api/user/cart:
    get:
      tags: [Cart]
      summary: Get User Cart
      description: Fetch the current user's cart. Returns an empty cart if none exists.
      security:
        - bearerAuth: []
      responses:
        "200":
          description: Cart retrieved successfully
          content:
            application/json:
              schema:
                type: object
                properties:
                  cart:
                    $ref: '#/components/schemas/Cart'
        "401":
          description: Unauthorized

    post:
      tags: [Cart]
      summary: Create or Update Cart
      description: Creates a new cart or overwrites the existing one for the user.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - items
              properties:
                items:
                  type: array
                  items:
                    $ref: '#/components/schemas/CartItem'
                totalPrice:
                  type: number
      responses:
        "201":
          description: Cart created successfully
        "200":
          description: Cart updated successfully
        "400":
          description: Invalid items array

  /api/user/cart/{id}:
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        description: The Cart ID
    put:
      tags: [Cart]
      summary: Update Cart by ID
      description: Update specific fields of a cart. (Owner or Admin only)
      security:
        - bearerAuth: []
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                items:
                  type: array
                  items:
                    $ref: '#/components/schemas/CartItem'
                totalPrice:
                  type: number
      responses:
        "200":
          description: Cart updated successfully
        "403":
          description: Forbidden - Not the owner or admin
        "404":
          description: Cart not found

  /api/admin/users:
    get:
      tags: [Admin Users]
      summary: Get All Users (Admin)
      description: Fetch paginated list of users (Admin only).
      security:
        - bearerAuth: []
      parameters:
        - in: query
          name: page
          schema:
            type: integer
            default: 1
        - in: query
          name: limit
          schema:
            type: integer
            default: 20
        - in: query
          name: search
          schema:
            type: string
          description: Search users by name or email
      responses:
        "200":
          description: Users fetched successfully
          content:
            application/json:
              schema:
                type: object
                properties:
                  users:
                    type: array
                    items:
                      $ref: '#/components/schemas/User'
                  meta:
                    type: object
                    properties:
                      total:
                        type: integer
                      page:
                        type: integer
                      limit:
                        type: integer
                      totalPages:
                        type: integer
        "401":
          description: Unauthorized
        "403":
          description: Forbidden (Admin only)

  /api/admin/users/{id}:
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        description: User ID
    get:
      tags: [Admin Users]
      summary: Get User by ID (Admin)
      description: Fetch a single user by ID (Admin only).
      security:
        - bearerAuth: []
      responses:
        "200":
          description: User fetched successfully
          content:
            application/json:
              schema:
                type: object
                properties:
                  user:
                    $ref: '#/components/schemas/User'
        "400":
          description: Invalid user ID
        "401":
          description: Unauthorized
        "403":
          description: Forbidden (Admin only)
        "404":
          description: User not found

    put:
      tags: [Admin Users]
      summary: Update User (Admin)
      description: Update user fields (Admin only).
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                name:
                  type: string
                email:
                  type: string
                role:
                  type: array
                  items:
                    type: string
                isVerified:
                  type: boolean
      responses:
        "200":
          description: User updated successfully
        "400":
          description: Invalid user ID or validation error
        "401":
          description: Unauthorized
        "403":
          description: Forbidden (Admin only)
        "404":
          description: User not found

    delete:
      tags: [Admin Users]
      summary: Delete User (Admin)
      description: Delete a user by ID (Admin only).
      security:
        - bearerAuth: []
      responses:
        "200":
          description: User deleted successfully
        "400":
          description: Invalid user ID
        "401":
          description: Unauthorized
        "403":
          description: Forbidden (Admin only)
        "404":
          description: User not found

    delete:
      tags: [Cart]
      summary: Delete Cart
      description: Delete a specific cart by ID. (Owner or Admin only)
      security:
        - bearerAuth: []
      responses:
        "200":
          description: Cart deleted successfully
        "401":
          description: Unauthorized
        "403":
          description: Forbidden
        "404":
          description: Cart not found